services:
  debian-lab:
    build: .
    container_name: proyecto_integrador_env
    volumes:
      - .:/workspace
      - /sys/fs/cgroup:/sys/fs/cgroup:rw      
    tty: true
    stdin_open: true
    restart: always
    privileged: true